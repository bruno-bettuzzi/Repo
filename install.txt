#!/bin/bash

su
cd
yum install -y epel-release

echo "Installation de suricata"
yum install -y suricata

echo"Installation du jdk"
yum install -y java-1.8.0-openjdk-headless.x86_64

echo "Installation de mongoDB"
#Script qui va écrire quelques lignes (voir graylog centos) dans le yum repo
yum install -y mongodb-org
systemctl enable mongod.service
systemctl start mongod.service

echo "Installation d'Elasticsearch"
rpm --import https://artifacts.elastic.co/GPG-KEY-elasticsearch
#Script qui va écrire quelques lignes (voir graylog centos) dans le yum repo
yum install -y elasticsearch
#Script conf ES (cf commentaire au dessus)
systemctl enable elasticsearch.service
systemctl restart elasticsearch.service

echo "Installation de graylog"
yum install -y pwgen
rpm -Uvh https://packages.graylog2.org/repo/packages/graylog-2.4-repository_latest.rpm
yum install -y graylog-server

yum install -y policycoreutils-python





###########OK ICI





installation database geo : 

wget http://geolite.maxmind.com/download/geoip/database/GeoLite2-City.tar.gz
gunzip GeoLite2-City.tar.gz
